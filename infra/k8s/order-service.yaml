apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: nexus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: order-service
          image: ghcr.io/kritsn/nexus-order-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: DB_HOST
              value: "postgres"
            - name: DB_NAME
              value: "nexus_order"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: nexus-db
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexus-db
                  key: POSTGRES_PASSWORD
            - name: EUREKA_URI
              value: "http://discovery-server:8761/eureka"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              memory: "768Mi"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: nexus
spec:
  selector:
    app: order-service
  ports:
    - port: 8085
      targetPort: 8085
